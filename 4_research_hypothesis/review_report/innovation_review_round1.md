# Innovation Review (model=qwen3-coder-next)

✅ **通过项**（符合DARIS质量评审标准，无需强制修改即可进入下一阶段）  
- **目标明确性**：三个创新点均精准对应文献中识别的空白（模糊图时序建模不足、残差突变建模缺失、小样本泛化差），且每个创新点均以“解决XX具体问题”为出发点，无泛化表述。  
- **可执行性**：所有改动文件路径精确到类/函数层级（如`FuzzyGCNLayer`、`KG2FuzzyRules()`），代码增量可控（<500行），且明确基于成熟开源框架（PyTorch Geometric/XGBoost/TimesNet），无“从零实现”风险。  
- **评估体系完整性**：四维指标（精度/不确定性/效率/鲁棒性）覆盖学术与工业双视角，且每个指标均有**量化目标值**（如PICP≥90%、模型≤5MB），可直接用于实验设计。  
- **风险预判与缓解方案**：每项风险均匹配具体技术手段（如L1正则化、FCM规则剪枝、ONNX+QAT部署），非泛泛而谈，具备可操作性。

---

⚠️ **风险项**（需在实验阶段重点监控，但**不构成必须修改**）  
| 创新点 | 风险项 | 监控建议 |
|--------|--------|----------|
| **点1** | 模糊参数过多导致收敛缓慢（尤其在小数据集上） | 监控训练损失曲线斜率；若>50轮无下降，启用早停+知识引导初始化 |
| **点2** | XGBoost规则覆盖率未达85%（若数据噪声高或突变点稀疏） | 预实验中用SHAP分析特征重要性；若关键特征缺失，补充滑动窗口高阶统计量（如skewness/kurtosis） |
| **点3** | KG迁移后零样本F1<0.75（若目标域设备与源域差异过大） | 构建跨域相似度度量（如设备振动频谱KL散度），用于筛选源域样本 |

> 📌 **注**：上述风险属于**技术实现层面的性能波动**，不影响方案可行性，可通过实验调参缓解，**不触发必须修改项**。

---

❌ **必须修改项**（违反DARIS核心原则，**需修正后方可进入评审**）  
| 问题类型 | 具体问题 | 修正要求 | 依据 |
|----------|----------|----------|------|
| **评估指标冲突** | 点2中“训练时间 ≤ 原版1.3×”与XGBoost两阶段训练流程矛盾 | **必须修改为**：<br>① 明确“原版”指**TimesNet单模型训练时间**（非TimesNet+XGBoost直连）；<br>② 补充说明：XGBoost训练时间独立计入（总时间 = TimesNet训练 + XGB训练 + 微调时间） | DARIS原则：**指标基准必须可复现、无歧义**。当前表述易被误解为“端到端联合训练”，但方案实际为两阶段，易引发评审质疑 |
| **技术可行性漏洞** | 点1中“模糊相似度计算（基于Zadeh t-norm）”与“可微分”要求冲突 | **必须修改为**：<br>① 删除Zadeh t-norm（不可微）；<br>② 改用**可微分模糊相似度**（如Dubois-Prade t-norm：`T(a,b) = max(0, a+b-1)`）；<br>③ 在`FuzzyGCNLayer`注释中声明：`# Dubois-Prade t-norm ensures ∂T/∂a, ∂T/∂b ∈ {0,1}` | DARIS原则：**核心算法必须满足可微分约束**。Zadeh t-norm在0/1边界不可导，将导致梯度消失，方案无法端到端训练 |
| **评估指标不可达成** | 点3中“10%样本下MAE ↓30% vs 单模型”缺乏理论支撑 | **必须修改为**：<br>① 补充**基线对比组**：`↓30%`需明确对比对象为**纯ANFIS**（非XGBoost）；<br>② 添加**置信区间**：`↓30% ±5% (95% CI)`；<br>③ 增加**样本量说明**：`n=50`（小样本场景定义） | DARIS原则：**量化目标需有统计显著性**。未限定对比基线与样本量，易被质疑夸大效果 |

---

### 📝 修正执行清单（按优先级排序）
1. **【高优】** 在`config/mtgnn_fgts.yaml`中添加注释：  
   `# fuzzy_similarity: 'dubois_prade' (不可用zadeh)`  
2. **【高优】** 在`train.py`中重写点2的训练时间统计逻辑：  
   ```python
   timesnet_time = train_timesnet(...)  # 记录TimesNet训练耗时
   xgb_time = train_xgb(...)            # 记录XGB训练耗时
   total_time = timesnet_time + xgb_time
   assert total_time <= 1.3 * timesnet_time, "XGB训练过慢！"
   ```
3. **【中优】** 在点3评估指标表格下方添加脚注：  
   `* MAE对比基线：纯ANFIS（无XGB修正）；样本量：n=50（目标域）；95% CI基于10次随机子采样`  
4. **【补充】** 在`kg/kg_anfis_loader.py`中增加KG规则完整性校验：  
   ```python
   if len(rules) < 5:
       logger.warning("KG规则过少，启用LLM补全模式")
       rules = llm_rule_completion(rules, kg_schema)
   ```

> ✅ **修正后即可通过DARIS终审**：上述修改均属**技术细节澄清与可行性加固**，不改变方案核心逻辑，且修改成本低（<2小时）。  
> 🔔 **特别提醒**：若未修正“Zadeh t-norm”问题，将导致**点1完全不可训练**，属致命缺陷。