# ==========================================
# DARIS - 科学研究工作流自动化系统
# 根目录 requirements.txt
# ==========================================
# Python 版本要求：>= 3.9
# PyTorch 版本：GPU (CUDA 11.8) 或 CPU
# ==========================================

# ==================== 核心依赖 ====================
requests>=2.31.0
PyYAML>=6.0.1

# ==================== 钉钉机器人 SDK ====================
dingtalk-sdk>=1.0.0

# ==================== Git 操作 ====================
GitPython>=3.1.40

# ==================== SSH 连接（阿里云同步） ====================
paramiko>=3.3.0
scp>=0.14.5

# ==================== 大模型调用（OpenAI 兼容 API） ====================
openai>=1.6.0
# 阿里云 DashScope 兼容 OpenAI API，无需额外安装

# ==================== 学术 API 调用 ====================
# CrossRef API - 无需密钥，使用 requests 直接调用
# arXiv API - 无需密钥，使用 requests 直接调用
# Semantic Scholar API
semantic-scholar>=0.3.0

# ==================== Zotero 集成 ====================
pyzotero>=1.6.0

# ==================== 数据处理 ====================
numpy>=1.24.0
pandas>=2.0.0

# ==================== 机器学习/深度学习 ====================
# XGBoost
xgboost>=2.0.0
# 时序预测库
tslearn>=0.6.0

# ==================== 实验评估 ====================
scikit-learn>=1.3.0
scipy>=1.11.0

# ==================== 可选依赖 ====================
# 进度条
tqdm>=4.66.0
# 配置文件热加载
watchdog>=3.0.0

# ==================== 开发依赖（可选） ====================
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.1.0
# mypy>=1.5.0

# ==================== PyTorch（单独安装） ====================
# GPU 版本 (CUDA 11.8) - 推荐
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 
# CPU 版本
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 注意：PyTorch 版本需要根据实际环境选择，不在此处固定